$('.slider-1').slick({
    autoplay: true,
    autoplaySpeed: 4000,
    dots: true,
    infinite: true,
    // adaptiveHeight: true,
    // variableWidth: true,
    // dotsClass: ''
    // appendDots:'dots-container'
     
});
